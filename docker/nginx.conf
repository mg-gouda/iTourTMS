upstream nextjs {
    server app:3000;
    keepalive 64;
}

server {
    listen 80;
    server_name _;

    # Serve uploaded files directly from the bind-mounted host directory.
    # This bypasses Next.js entirely, which avoids the standalone 404 problem
    # (Next.js only serves public/ files that existed at build time).
    location /uploads/ {
        alias /uploads/;
        expires 30d;
        add_header Cache-Control "public, immutable";
        add_header X-Content-Type-Options nosniff;
        # These bytes came from a user and nginx serves them from our own
        # origin, bypassing Next.js — so the app's CSP in next.config.ts never
        # applies here. Neutralise active content at the point of delivery.
        add_header Content-Security-Policy "default-src 'none'; img-src 'self'; style-src 'unsafe-inline'; sandbox";
        add_header X-Frame-Options DENY;
        access_log off;
        try_files $uri =404;
    }

    # Everything else → Next.js
    location / {
        proxy_pass         http://nextjs;
        proxy_http_version 1.1;
        proxy_set_header   Upgrade $http_upgrade;
        proxy_set_header   Connection "upgrade";
        proxy_set_header   Host $host;
        proxy_set_header   X-Real-IP $remote_addr;
        proxy_set_header   X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header   X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
        proxy_read_timeout 60s;
        proxy_send_timeout 60s;
        client_max_body_size 15m;
    }
}
